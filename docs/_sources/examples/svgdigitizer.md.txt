# SVG Digitizer

Metadata for digitized data from SVG figures. See the [schema reference](../schema/svgdigitizer/index) for detailed field descriptions.

```{literalinclude} ../../examples/file_schemas/svgdigitizer.yaml
:language: yaml
```
